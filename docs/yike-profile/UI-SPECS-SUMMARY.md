# 一刻相册 5 主 Tab UI Spec 对照（2026-08-14 ui-spec 重新生成）

用 ui-spec skill(scripts/ui-spec.py + qwen 多模态)对 docs/yike-profile/tab_{album,camera,creative,find,photo}.png
5 张一刻相册真机截图重新生成结构化 JSON，落盘 docs/ui-specs/tab_*.spec.json。
本文档汇总各 Tab 的 spec 区块 vs 当前前端实现(AiTabs.kt/MediaListScreen.kt)差距，
作为增量重构依据。**原则：只做低风险增量补齐，不推翻已多轮打磨的对齐实现。**

## 相册 Tab（tab_album.spec.json）
spec 区块：
- 顶部状态栏/备份通知栏/功能图标区
- 夏日活动轮播图 Banner
- 一键创建相册标题栏
- 左侧相册卡片 + 右侧智能搜图成册卡(智能搜图标题行/个人证件/手机截屏/一日三餐胶囊)
- 云空间相册标题与操作栏(新建/排序按钮)
- 筛选标签栏(全部/我的/共享/新增)
- 悬浮添加按钮
- 底部导航栏
当前(AiTabs AlbumTabScreen)：智能搜图成册浅蓝卡+白胶囊/云空间相册/人物/筛选(全部我的共享)/活动Banner/一键创建入口。
**差异**：① 智能搜图成册内部 spec 是静态预设类别(个人证件/截屏/三餐)，当前是 AI 动态场景(albums)——设计方向差异非 bug，保留动态更优。② 悬浮添加按钮待确认是否需补。
**状态**：基本已对齐，无必须改动。

## 查找 Tab（find.spec）
spec 区块：
- 顶部状态栏/功能操作栏(备份胶囊)
- 人物相册分类(头像列表)
- 足迹地图统计(点亮地图/新增城市角标)
- 事物相册分类(风景/美食/玩乐/生活记录卡片)
- 类型相册分类(截图/动态照片)
- **数据安全提示条**
- 底部导航栏
当前实现（SearchTabScreen）：搜索框/场景(横排卡)/人物(圆头像+更多)/足迹(地图统计卡)/类型(截图视频动态照片)。
**差异（待补）**：① **数据安全提示条** spec 底部有，当前无。② spec 有"事物相册分类"(风景/美食/玩乐/生活记录)，当前"场景"来自 AI albums(=auto相册)，概念相近但缺独立的事物区。

## 创意 Tab（creative.spec.json）
spec 区块：
- 顶部状态栏/广告横幅区域(左上作品集入口+右侧文案+一键获取好礼)
- 核心功能网格(8功能:时光足迹/人像美颜/AI消除/AI动图/创意拼图/一键成片/朋友圈9图/全部工具)
- 主要操作按钮(AI改图/导入图片)
- **夏日玩法推荐区域(标题+横向滚动卡片 推荐1/2/3)**
当前（CreativeTabScreen）：Banner/双主按钮/8工具网格/视频列表。
**缺失（待补）**：**夏日玩法推荐横向卡片区**。作品集入口待确认。

## 拍摄 Tab（camera.spec.json）
spec 是**系统相机界面**(左上返回/3:4比例/闪光灯/快门)。当前拍摄=CenterCameraFab 拉起系统相机，不走 Tab 页，spec 仅为参考印证相机方向正确。
**状态**：确认无误，无需改动。

## 照片 Tab（photo.spec.json）
spec 区块：
- 顶部状态栏/顶部 Banner(电视剧宣传/搜索/红包/头像)
- 功能操作卡(左备份/右清理)
- **本机/云空间切换标签栏**
- 时间轴与筛选区域(「按时间|按相册」)
- 2026/2025 时间轴照片网格
- 底部导航
当前（MediaListScreen 照片 Tab）：Photo-over look timeline + 功能卡(备份/清理) + Banner + 按时间|相册切换。
**差异**：spec 多了"本机/云空间切换标签栏"，当前是否已实现需核对 MediaListScreen（记忆第35轮提过本机/云文字切换）。

---

## 本轮重构范围（低风险增量）
1. **CreativeTabScreen**：补「夏日玩法推荐」横向滚动卡片区（一日推荐卡片×3）。
2. **SearchTabScreen**：补「数据安全提示条」。
3. 照片 Tab 核对「本机/云」切换标签栏是否已实现。

其余按 spec 已对齐，不随意改动。